package com.gruppe4.wlan_detektor.ui.Utility.Datenbank.Repository


import kotlinx.coroutines.Dispatchers
import kotlinx.coroutines.withContext

/*
class AppRepository
{
    private val datenbankTabellen:DatenbankWlanDetektor

    init {
        val db = DatenbankWlanDetektor.createInstance(application = DatenbankWlanDetektor = db.datenbankTabelle
    }

    // Methoden implementieren
    suspend fun inser(datenbankWlanDetektor: DatenbankWlanDetektor) {
        withContext(Dispatchers.IO)
        {
            datenbankTabellenDao.insert(DatenbankTabellen)
        }
    }
}

 */